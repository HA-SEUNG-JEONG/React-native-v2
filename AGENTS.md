# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# 학습 퀴즈 결과 기록 형식

`docs/*-study-guide.md` 복습 퀴즈 결과를 `docs/`에 기록할 때 (`/doc` 사용), 요약 표만으로는 부족 — 다음 구조 따를 것 (`docs/2026-08-28-p1-quiz-review.md` 참고):

- **문항별 상세 섹션**: 내 답(원문 그대로) → 문제점(왜 틀렸는지, "결과는 맞는데 이유 누락" 같은 패턴 포함) → 정답(코드 스니펫 포함) → 암기 고리(다음에 헷갈리지 않을 한 줄 비유/규칙) → 가이드 근거 절 번호.
- **종합 약점 패턴 섹션**: 개별 오답 나열 넘어서, 오답들의 공통점 파악 ("무엇은 맞히는데 왜를 못 댐", "직관적으로 그럴듯한 오답에 낚임", "가이드 뒷부분 세부 절에서 약함" 등).
- **재복습 일정 섹션**: 완전히 모른 항목(1차, 3일 내) vs 부분정답/오답(2차, 1주 내)로 우선순위 나눠 제시.

요약 표만 있는 형식(문항/주제/결과 3열)은 재복습 시 원문 다시 찾아야 해서 불충분 — 반드시 상세 구조로 작성.
